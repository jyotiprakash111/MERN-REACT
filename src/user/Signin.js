import React, { useState } from 'react';
import Base from '../core/Base';
import { Link } from 'react-router-dom';


const Signin = () => {
    return (
        <Base title="Signin up Page" description="A Page For User Signup!">
            <h1>Signin works</h1>
        </Base>
    );
};

export default Signin;